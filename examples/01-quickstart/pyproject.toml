[project]
name = "01-quickstart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["sqlalchemy>=2", "starlette-admin", "uvicorn[standard]"]

[tool.uv.sources]
starlette-admin = { workspace = true, editable = true }
