[project]
name = "04-user-registration"
version = "0.1.0"
description = "Public sign-up page built on CustomView + AuthProvider"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "passlib[bcrypt]>=1.7.4",
    "sqlalchemy>=2",
    "starlette-admin",
    "uvicorn[standard]",
]

[tool.uv.sources]
starlette-admin = { workspace = true, editable = true }
